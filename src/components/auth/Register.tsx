import React from 'react';
import { TextField, Button } from '@material-ui/core'

const Register = () => {
  return (
    <div>
        <TextField />
        <TextField />
        <Button>Login</Button>
    </div>
  );
}

export default Register;