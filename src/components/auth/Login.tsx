import React from 'react';
import { TextField, Button } from '@material-ui/core'

const Login = () => {
  return (
    <div>
        <TextField />
        <TextField />
        <Button>Login</Button>
    </div>
  );
}

export default Login;